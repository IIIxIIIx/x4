#include <iostream>// РАКОВИЧ АЛЕКСЕЙ. П43021.
using namespace std;
int main() {
	setlocale(LC_ALL, "Russian");
	//1. Из двух чисел с разной четностью вывести на экран нечетное число
	 /* int x, i;
	cin >> x >> i;
	 if (x < i);
	 else if (i % 2 == 1);{
						i++;
						 }
	cout << i << endl;
	return 0;
	}

	//3. По номеру года (от 0 до 2021) определить, этот год високосный или нет.

	int y;
	cin >> y;
	if ((y%4 == 0 && y %100 !=0)|| y % 400 == 0){
	 cout << "високосный \n";
	}
	else {
	 cout << "не високосный \n";
	}


	//2. Пользователь вводит целое число. Надо вывести на экран положительное оно, отрицательное или ноль.
		int x;
		cin >> x;
		if (x == 0);
		cout << "0\n";
		if (x < 0);
		cout << " отрицательное\n";
		if (x > 0);
		cout << "положительное\n";*/




		//4. Вводятся два целых числа не равных нулю. Проверить делится ли первое на второе. Вывести на экран сообщение об этом, а также остаток (если он есть) и частное 
	int a, b,c;
	if (a != 0 || b != 0);
	if (c = a / b);
	if (a % b == 0);
	else cout << "Число a делится на b без остатка\n";
	if (b % a == 0);
	else cout << "Число b делится на a без остатка\n";
	return 0;
}